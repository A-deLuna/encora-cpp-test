#pragma once

#include <stdint.h>
#include <vector>
void find_largest_sum(const uint32_t T,
                      const std::vector<uint32_t>& I,
                      std::vector<uint32_t>& M,
                      uint32_t& S);
int add(int a, int b);
